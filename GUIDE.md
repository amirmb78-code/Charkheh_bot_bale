<div dir="rtl">

# 📖 راهنمای کامل راه‌اندازی و استفاده از «چرخه» (Charkheh)

این راهنما از صفر تا صد است: از ساخت ربات در بله تا دریافت اولین سفارش واقعی.

---

## فهرست

1. [پیش‌نیازها](#قسمت-صفر--پیشنیازها)
2. [ساخت ربات در بله و گرفتن توکن](#قسمت-یک--ساخت-ربات-در-بله)
3. [پیدا کردن چت‌آیدی ادمین](#قسمت-دو--پیدا-کردن-چتآیدی-ادمین)
4. [تنظیم فایل محیطی `.env`](#قسمت-سه--تنظیم-env)
5. [اجرا با Docker](#قسمت-چهار--اجرا-با-docker)
6. [ساخت جداول دیتابیس](#قسمت-پنج--ساخت-جداول)
7. [اتصال وب‌هوک](#قسمت-شش--اتصال-وبهوک)
8. [تست کامل به‌عنوان مشتری](#قسمت-هفت--تست-endtoend)
9. [کار ادمین داخل بله](#قسمت-هشت--گفتگوی-ادمین-در-بله)
10. [داشبورد وب](#قسمت-نه--داشبورد-وب)
11. [دستورهای مشتری](#قسمت-ده--دستورهای-مشتری)
12. [نگهداری، بکاپ و عیب‌یابی](#قسمت-یازده--نگهداری-و-عیبیابی)

---

## قسمت صفر — پیش‌نیازها

| نیاز | حداقل | توضیح |
|---|---|---|
| سرور | ۱ هسته / ۱ گیگ رم | VPS داخل ایران پیشنهاد می‌شود (تأخیر کمتر با بله) |
| دامنه + HTTPS | الزامی | بله فقط به آدرس HTTPS وب‌هوک وصل می‌شود |
| Docker | آخرین نسخه | ساده‌ترین راه اجرا (یا Node.js 20+ بدون Docker) |
| حساب بله | — | برای ساخت ربات و چت ادمین |

> 💡 **برای تست روی کامپیوتر شخصی** می‌توانید از تونل استفاده کنید: `ngrok http 3000` و آدرس `https://....ngrok-free.app` را در `APP_BASE_URL` بگذارید.

---

## قسمت یک — ساخت ربات در بله

1. در بله به **بات‌فادر** پیام بدهید: [@BotFather](https://ble.ir/BotFather)
2. دستور `/newbot` را بفرستید و نام و یوزرنیم ربات را بسازید (مثلاً `@charkheh_bot`)
3. **توکن** ربات را کپی کنید (رشته‌ای شبیه `123456:ABC-DEF...`)

این توکن همان `BALE_BOT_TOKEN` است. آن را جایی امن نگه دارید و **در هیچ چتی نفرستید**.

---

## قسمت دو — پیدا کردن چت‌آیدی ادمین

چت‌آیدی یک **عدد** است (مثل `123456789`) — نه یوزرنیم.

سریع‌ترین راه:
1. فعلاً توکن ربات را در `.env` بگذارید و برنامه را اجرا کنید (یا حتی بدون اجرا)
2. در بله به ربات خود یک پیام (هر چیزی) بفرستید
3. این دستور را اجرا کنید:

```bash
curl -s "https://tapi.bale.ai/bot<توکن>/getUpdates" | head -c 2000
```

4. در خروجی، مقدار `"chat":{"id":123456789` را پیدا کنید — همان چت‌آیدی شماست.

چند ادمین دارید؟ همه را با ویرگول: `ADMIN_CHAT_IDS="123456789,987654321"`

---

## قسمت سه — تنظیم `.env`

```bash
unzip charkheh-bot-bale.zip && cd charkheh-bot-bale   # یا مسیر پوشه پروژه
cp .env.example .env
nano .env
```

| متغیر | مقدار نمونه | توضیح |
|---|---|---|
| `DATABASE_URL` | `postgresql://charkheh:...@db:5432/charkheh_db` | با Docker خالی بماند؛ compose خودش می‌گذارد |
| `BALE_BOT_TOKEN` | توکن قسمت یک | الزامی |
| `ADMIN_CHAT_IDS` | `123456789` | چت‌آیدی قسمت دو؛ برای چند ادمین با `,` |
| `ADMIN_DASHBOARD_PASSWORD` | یک رمز قوی | ورود به /admin |
| `ADMIN_SESSION_SECRET` | `openssl rand -hex 32` | امضای کوکی نشست |
| `APP_BASE_URL` | `https://charkheh.example.com` | دامنه HTTPS بدون اسلش آخر |
| `BALE_WEBHOOK_SECRET` | `openssl rand -hex 24` | بدون این، آپدیت‌ها پردازش نمی‌شوند |

---

## قسمت چهار — اجرا با Docker

```bash
docker compose up -d --build
docker compose logs -f app     # دیدن لاگ‌ها (Ctrl+C برای خروج)
```

چک سلامت: `curl https://YOUR_DOMAIN/api/health` → باید `{"ok":true}` برگردد.

> بدون Docker؟ `npm install && npm run build && npm start` و جلوی آن Nginx/Caddy با HTTPS بگذارید.

---

## قسمت پنج — ساخت جداول

فقط بار اول (و بعد از هر آپدیت اسکیما):

```bash
docker compose exec app npx drizzle-kit push
```

باید ۷ جدول ساخته شود: `customers`، `pickup_requests`، `request_events`، `conversation_states`، `material_rates`، `credit_transactions`، `bot_settings`

---

## قسمت شش — اتصال وب‌هوک

1. مرورگر: `https://YOUR_DOMAIN/admin` → رمز داشبورد را بزنید
2. در کارت «اتصال ربات بله» دکمه **«اتصال وب‌هوک»** را بزنید
3. اگر `✅ وب‌هوک با موفقیت وصل شد` دیدید، تمام است.

سکرت به بله اعلام شده و هر آپدیت امضاشده می‌آید؛ آپدیت جعلی ۴۰۱ می‌گیرد.

---

## قسمت هفت — تست end-to-end

### نقش مشتری (با گوشی خودتان در بله):

1. `/start` → پیام خوش‌آمد + قوانین حریم خصوصی
2. **یک عکس** از کالا بفرستید → دکمه‌های نوع ظاهر می‌شود
3. نوع را بزنید (مثلاً «پلاستیک ♻️») → دکمه‌های وزن می‌آیند
4. وزن تقریبی را بزنید → **برآورد خودکار قیمت** را می‌بینید
5. چند ثانیه بعد (وقتی ادمین قیمت داد) پیام قیمت با **تایید/انصراف** می‌رسد
6. «تایید می‌کنم» → **۳۱ استان** را دکمه‌ای انتخاب کنید
7. دکمه **«📍 ارسال موقعیت روی نقشه»** را بزنید و پین را بفرستید (یا آدرس را تایپ کنید)
8. شهر + آدرس دقیق را بنویسید (مثلاً: `تهران، خیابان آزادی، کوچه گلها، پلاک ۱۲`)
9. **«📱 ارسال شماره تماس»** یا «بدون شماره، ادامه»
10. زمان را انتخاب کنید → ✅ هماهنگ شد

### نقش ادمین (همان لحظه‌ها):

1. عکس + نوع + وزن + **برآورد سیستم** (+ پرچم ⚠️ کم‌ارزش اگر زیر حد اعزام) می‌رسد
2. قیمت بدهید: دکمه «💰 ثبت قیمت» یا بنویسید `قیمت 1 85000`
3. بعد از هماهنگی، پیام «آماده جمع‌آوری» با **دکمه نقشه** و شماره تماس می‌رسد
4. سر درب: «انجام 1» → `وزن 1 ۴.۵` → `مبلغ 1 ۹۲۰۰۰`
5. مشتری فاکتور کامل + پاداش کیف پول + CO₂ + دکمه امتیاز ۱–۵ می‌گیرد و آدرس/عکس **خودکار پاک می‌شود**

---

## قسمت هشت — گفتگوی ادمین در بله

| دستور/اکشن | کاربرد |
|---|---|
| `قیمت <شناسه> <مبلغ>` | ارسال قیمت پیشنهادی (مثلاً `قیمت 12 ۸۵۰۰۰`) |
| `رد <شناسه>` | انتقال به نوبت جمع‌آوری تجمیعی (کم‌ارزش) |
| `انجام <شناسه>` | ثبت مراجعه حضوری |
| `وزن <شناسه> <عدد>` | ثبت وزن (اعشار فارسی هم قبول: `۴.۵`) |
| `مبلغ <شناسه> <عدد>` | ثبت مبلغ نهایی و تکمیل سفارش |
| `/stats` | آمار کامل (تکمیل، بازگشت، امتیاز، CO₂، مبالغ) |
| `/broadcast <متن>` | پیام همگانی به همه مشتریان |
| `/help` | همین راهنما در چت |

> نکته: دستورهای سریع وابسته به «حالت گفتگو» نیستند؛ دو ادمین می‌توانند هم‌زمان روی سفارش‌های مختلف کار کنند. تکرار یک اکشن با پیام مؤدبانه رد می‌شود.

---

## قسمت نه — داشبورد وب (`/admin`)

- **داشبورد**: کارت‌های کل سفارش‌ها، نرخ تکمیل، نرخ بازگشت، رضایت ⭐، مجموع خرید/پاداش، CO₂ + وضعیت و دکمه‌های وب‌هوک
- **سفارش‌ها**: فیلتر وضعیت + جزئیات کامل (نقشه، عکس امن از `/admin/media`، تاریخچه رویداد، کیف پول مشتری، تغییر دستی وضعیت برای موارد اضطراری)
- **تنظیمات**:
  - نرخ هر کیلو + ضریب CO₂ هر ماده → مستقیم روی «برآورد خودکار» اثر دارد
  - حداقل ارزش اعزام فوری (پیش‌فرض ۵۰٬۰۰۰ تومان)
  - درصد پاداش مشتری (پیش‌فرض ۱۲٪) و پاداش دعوت (پیش‌فرض ۲۰٬۰۰۰ تومان)
  - محدوده پوشش: پیش‌فرض «سراسر ایران» — برای پایلوت محلی، فقط استان‌های فعال را تیک بزنید

---

## قسمت ده — دستورهای مشتری

| دستور | کاربرد |
|---|---|
| `/start` | شروع / شروع دوباره (حالت گیرکرده پاک می‌شود) — با کد: `/start CHK-XXXX` |
| `CHK-XXXXXX` | ثبت کد دعوت دوستان |
| `/status` | آخرین سفارش‌ها |
| `/prices` | تعرفه‌های روز |
| `/balance` | کیف پول اعتباری + آخرین تراکنش‌ها |
| `/invite` | کد دعوت شخصی + شرح پاداش |
| `/help` | راهنما و سیاست حریم خصوصی |
| `/cancel` یا `/لغو` | پاک‌کردن جریان جاری |

---

## قسمت یازده — نگهداری و عیب‌یابی

### بکاپ (روزانه پیشنهاد می‌شود)
```bash
docker compose exec db pg_dump -U charkheh charkheh_db > backup-$(date +%F).sql
docker run --rm -v charkheh-bot-bale_uploads:/d -v $PWD:/b alpine tar czf /b/uploads-$(date +%F).tgz -C /d .
```

### آپدیت نسخه
```bash
git pull && docker compose up -d --build && docker compose exec app npx drizzle-kit push
```

### عیب‌یابی

| علامت | علت محتمل | راه‌حل |
|---|---|---|
| آپدیت‌ها نمی‌رسند | وب‌هوک وصل نیست | داشبورد → «اتصال وب‌هوک»؛ APP_BASE_URL باید HTTPS باشد |
| لاگ: `unauthorized` | سکرت اشتباه/قدیمی | یک بار «قطع» و دوباره «اتصال» بزنید |
| لاگ: سکرت تنظیم نشده | `BALE_WEBHOOK_SECRET` خالی | مقدار بدهید و ری‌استارت کنید |
| عکس‌ها 404 | پس از تکمیل پاک شده‌اند | طبیعی است (حریم خصوصی) — قبل از تکمیل چک کنید |
| «DATABASE_URL تنظیم نشده» | env نرسیده | در compose فایل `.env` کنار `docker-compose.yml` باشد |
| `/api/health` → `ok:false` | دیتابیس قطع | `docker compose ps` و لاگ db را ببینید |
| ربات جواب نمی‌دهد ولی health ok | توکن اشتباه | توکن را از BotFather دوباره بگیرید |

### تست خودکار قبل از هر دیپلوی
```bash
npm install && npm run test:flow   # ۴۲ سناریوی کامل بدون نیاز به بله
```

---

✅ **چک‌لیست راه‌اندازی نهایی**: توکن ✓ چت‌آیدی ادمین ✓ env ✓ جداول ✓ وب‌هوک ✓ یک سفارش تستی کامل ✓ بکاپ راه افتاده ✓

</div>
